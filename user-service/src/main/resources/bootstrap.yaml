
spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

microservice-car:
  url: http://car-service/api/car

microservice-bike:
  url: http://bike-service/api/bike