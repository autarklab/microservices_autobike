server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: 05apiGateway
          uri: https://github.com/autarklab/microservices_autobike
          search-paths: config-data
  application:
    name: config-service
